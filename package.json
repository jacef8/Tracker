{
  "name": "groundlink",
  "version": "1.0.0",
  "description": "Live field map PWA — satellite imagery, shared GPS, pins, and group chat",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
