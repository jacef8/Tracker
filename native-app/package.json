{
  "name": "groundlink-native",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for GroundLink with native background GPS. Separate from the web/server project — has nothing to do with the Railway deploy.",
  "scripts": {
    "add:android": "cap add android",
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  },
  "dependencies": {
    "@capacitor-community/background-geolocation": "^1.2.20",
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/haptics": "^6.0.2",
    "@capacitor/share": "^6.0.3"
  }
}
